import io.std;

let nums: []i64 = [1, 2, 3, 4, 5];

func printArray(arr: []i64) {
    for let i = 0; i < arr._len; i++ {
        std.PutInt(arr[i]);
    }
}

func main() {
    std.PutStr("Hello, world!\n");
    printArray(nums);

    std.PutInt(abs(-20));
}