import io.std;

func ([]int).print() {
    for let i = 0; i < self._len; i++ {
        if i > 0 {
            std.puts(", ");
        }
        
        std.putint((*self)[i]);
    }

    std.putr('\n');
}

func main() {
    let x = [1, 2, 3, 4, 5];
    std.puts("x = ");
    x.print();
}