import io.std;

func main() {
    #if OS == "windows":
        std.puts("Windows\n");
    #end

    #if ARCH == "amd64" || ARCH == "i386" || ARCH == "arm64":
        std.puts("64 bit architecture\n");
    #end

    #if !DEBUG:
        std.puts("Debug\n");
    #end

    #if COMPILER == "berryc" && ARCH_SIZE != "32":
        std.puts("berryc - x64\n");
    #end
}