module core;

type byte = u8;
type rune = i32;

/* ------------------------------------------------------------------------- */

pub func abs(n: i64) i64 {
    if n < 0 {
        return -n;
    }

    return n;
}