#if OS == "windows" {
    // Windows only
} else { 
    // Unix, etc.
}

// Can also be applied at the file-level: everything after this line won't //
compile if OS != "windows".
#require OS == "windows";

// For fancy stuff with braces (can use #begin and #end instead of {})
#if MY_VAR #begin
{
#end

// -- snip --

#if MY_VAR #begin
}
#end
